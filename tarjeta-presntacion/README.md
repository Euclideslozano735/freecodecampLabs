Deberías tener un div con un atributo class con un valor de business-card que contenga todos los otros elementos.
Dentro del elemento .business-card, debe haber un elemento img con un atributo class con un valor de profile-image. Puedes establecer la fuente de la imagen en https://cdn.freecodecamp.org/curriculum/labs/flower.jpg si lo deseas. Debe haber un alt con una descripción significativa.
Dentro del elemento .business-card, después del elemento img, debería haber tres elementos p con un atributo class que tenga, respectivamente, un valor de full-name, designation, y company.
El primer elemento p debe contener tu nombre.
El segundo elemento p debe contener tu designación.
El tercer elemento p debe tener el nombre de tu empresa.
Debe haber un elemento hr debajo del tercer elemento p.
Después del elemento hr, debe haber dos elementos p. En el primer elemento p, deberías tener una dirección de correo electrónico como texto. En el segundo elemento p, el texto debe ser un número de teléfono.
Después de los dos elementos p, debe haber un elemento a con una clase portfolio-link, el texto Portfolio y debe enlazar a una URL válida.
Debe haber un elemento hr después del elemento a que contiene el enlace del portafolio.
Deberías tener otro elemento div con un atributo class con un valor de social-media. Dentro de este elemento, debe haber un elemento h2 con el texto Connect with me.
Dentro del os elementos .social-media, debe haber tres elementos a. Los elementos a deben tener el texto Twitter, LinkedIn y GitHub respectivamente con enlaces a sitios web válidos.

<!-- Deberías enlazar la hoja de estilo styles.css usando la etiqueta link en la sección head del archivo HTML.  -->

El fondo de tu página debería ser rosybrown. La fuente general debe ser Arial, con una alternativa de sans-serif.
El selector .business-card debería tener propiedades para establecer el elemento con un ancho de 300px y un color de fondo a tu elección. También deberías establecer un padding de 20px alrededor y un margen superior de 100px. El texto debería estar alineado al centro y el tamaño de la fuente debería ser 16px. Los márgenes izquierdo y derecho deben establecerse en auto.
El selector .profile-image debería tener una propiedad max-width con un valor de 100%.
Todos los elementos p deben tener un margen superior e inferior de 5px.
Todos los enlaces en la página no deberían estar subrayados.
